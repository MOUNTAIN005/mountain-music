{
  "framework": "nextjs",
   "buildCommand": "node prisma/prepare-db.js && prisma generate --schema=prisma/schema.pg.prisma && prisma db push --accept-data-loss --skip-generate && next build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "env": {
    "NEXT_PRIVATE_STANDALONE": "true"
  }
}
